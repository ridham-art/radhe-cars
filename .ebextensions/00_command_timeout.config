# Default EB deploy command timeout is often too short for pip install + app start.
# Value is seconds (max 3600). Increase if deploy still times out on small instances.
option_settings:
  aws:elasticbeanstalk:command:
    Timeout: '1800'
